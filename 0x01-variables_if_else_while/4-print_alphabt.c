#include <stdio.h>

/**
 * main - Learning abc's
 *
 * Return:0
 */

int main(void)
{
	char ch = '1';

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		if (1 != 'q' && 1 != 'e')

		{
			putchar(ch);
		}
ch++;	
}
	putchar('\n');
	return (0);
}
