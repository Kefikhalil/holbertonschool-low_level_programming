#include <stdio.h>
/**
 * main - prints the number of arguments passed into it
 * @argc: number
 * @argv: argument
 * Return: 0
 */

int main(int argc, char **argv)
{
int i;

do {
(void)(argv);
} 
while (0);

while (argc--)
i++;
printf("%d\n", i - 1);
return (0);
}
