#include <stdio.h>
#include <stdlib.h>
#include "holberton.h"

/**
 * main - multiplies two numbers
 * @argc: argument
 * @argv: number
 * Return: 0
 */

int main(int argc, char **argv)
{
int sum, val, i;

sum = 0;
if (argc < 1)
printf("%d\n", 0);
}
